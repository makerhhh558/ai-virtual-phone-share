/* ═══ 灰蓝薄雾美化 ═══ */

/* ── 头部：返回按钮 ── */
.page-header-content > .page-back-btn:first-of-type svg {
  transform: rotate(-90deg) scale(1.2);   /* 逆时针90°并放大1.2倍 */
  display: block;
}

/* ── 头部：更多按钮 ── */
.page-header-right .page-back-btn svg {
  display: none;
}

.page-header-right .page-back-btn {
  background-image: url('https://i.ibb.co/SX5QY014/1781265258764.png');
  background-size: contain;
  background-repeat: no-repeat;
  background-position: center;
  width: 22px;
  height: 22px;
  padding: 0;
  border: none;
  background-color: transparent;
  cursor: pointer;
}

/* ── 头部：昵称及附加文字 ── */
.page-title {
  font-size: 14px;
  line-height: 1.2;
  color: #555555;               /* 昵称深灰色 */
}

.page-title::before {
  content: 'Écoute Chérie';
  display: block;
  font-size: 15px;
  font-weight: normal;
  color: #252525;               /* 上方文字黑色 */
  margin-bottom: 7px;
}

.page-title::after {
  content: '关注';
  display: inline-block;
  font-size: 12px;
  color: #ffffff;
  background: rgba(120, 120, 120, 0.35);   /* 半透明浅灰底 */
  backdrop-filter: blur(5px);              /* 毛玻璃模糊 */
  -webkit-backdrop-filter: blur(5px);      /* Safari 兼容 */
  padding: 2px 6px;
  border-radius: 10px;
  margin-left: 6px;
  line-height: 1;
  vertical-align: middle;
  position: relative;
  top: -1px;
}

/* ── 消息包装器：头像在上，气泡在下 ── */
.chat-msg-wrapper[data-role="assistant"] {
  flex-direction: column;
  align-items: flex-start;
  gap: 4px;
}
.chat-msg-wrapper[data-role="user"] {
  flex-direction: column-reverse;
  align-items: flex-end;
  gap: 4px;
}

/* ── 头像：方形 15px 圆角 + 外侧底部对齐文字 ── */
.chat-msg-avatar {
  position: relative;
  overflow: visible !important;
  border-radius: 15px !important;
}
.chat-msg-avatar > * {
  border-radius: 15px !important;
}

/* 用户头像装饰文字 */
.chat-msg-wrapper[data-role="user"] .chat-msg-avatar::before {
  content: "@Silence: ";                          /* 👈此处可修改文字 */
  position: absolute;
  bottom: 0;
  right: 100%;
  margin-right: 6px;
  font-size: 13px;
  color: #9CA3AF;                                /* 👈此处可修改颜色 */
  letter-spacing: 0.5px;
  white-space: nowrap;
}

/* AI 头像装饰文字 */
.chat-msg-wrapper[data-role="assistant"] .chat-msg-avatar::after {
  content: "@Possession: ";                          /* 👈此处可修改文字 */
  position: absolute;
  bottom: 0;
  left: 100%;
  margin-left: 6px;
  font-size: 13px;
  color: #9CA3AF;                                /* 👈此处可修改颜色 */
  letter-spacing: 0.5px;
  white-space: nowrap;
}

/* ── AI 气泡（普通文本消息） ── */
.chat-bubble-role-assistant:not(.chat-bubble-media):not(.voice-msg-bubble) {
  background-color: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(1px);
  -webkit-backdrop-filter: blur(1px);
  border: 1px solid #D1D5DB;
  border-radius: 20px;
  color: #4B5563;
  padding: 5px 10px;                              /* 👈此处可调内边距 */
}

/* ── 用户气泡（普通文本消息） ── */
.chat-bubble-role-user:not(.chat-bubble-media):not(.voice-msg-bubble) {
  background-color: #252525;
  border: 1px solid #3F3F46;
  border-radius: 20px;
  color: #F9FAFB;
  padding: 5px 10px;                              /* 👈此处可调内边距 */
}

/* ── 连续发言隐藏头像 ── */
.chat-msg-wrapper[data-consecutive] .chat-msg-avatar {
  display: none;
}

/* ── 关闭框架默认间距 ── */
.chat-scroll-anchored > .flex.flex-col.gap-4 {
  gap: 0 !important;
}

/* ═══ 间距控制 ═══ */
.chat-msg-wrapper {
  margin-top: 0;
  margin-bottom: 0;
}
.chat-msg-wrapper[data-role="system"] {
  margin-top: 6px;
  margin-bottom: 6px;
}
.chat-msg-wrapper[data-role="user"],
.chat-msg-wrapper[data-role="assistant"] {
  margin-top: 0;
  margin-bottom: 0;
}
.chat-msg-wrapper[data-consecutive] {
  margin-top: -13px;                              /* 👈连续发言间距 */
}
.chat-msg-wrapper[data-role="assistant"]:not([data-consecutive]),
.chat-msg-wrapper[data-role="user"]:not([data-consecutive]) {
  margin-top: 14px;                               /* 👈分组间距 */
}

/* ── 心声按钮 ── */
.chat-monologue-heart {
  margin-left: 0 !important;
  align-self: flex-start !important;
  margin-top: 2px !important;
  display: flex;
  align-items: center;
  gap: 6px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  transform: none !important;          /* 禁止缩放 */
  transition: none !important;         /* 取消过渡动画 */
}
.chat-monologue-heart::after {
  content: "That's why I'm with you.";            /* 👈此处可修改文字 */
  font-size: 11px;
  color: #9CA3AF;                                 /* 👈此处可修改颜色 */
  letter-spacing: 0.5px;
  white-space: nowrap;
  opacity: 0.8;
}
.chat-monologue-heart:active,
.chat-monologue-heart:focus-visible {
  transform: none !important;          /* 点击时不变大 */
}

/* ── 内心独白卡片（合并整理） ── */
/* 隐藏胶带装饰 */
.chat-thought-tape-left,
.chat-thought-tape-right {
  display: none;
}

/* 卡片容器：背景、模糊、边框完全对齐 assistant 气泡 */
.chat-thought-card {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(1px);
  -webkit-backdrop-filter: blur(1px);
  border: 1px solid #D1D5DB;
  border-radius: 16px;
  padding: 12px 16px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.04);
  align-self: flex-start !important;
  margin-top: 0 !important;
  margin-left: 0px !important;
  width: 100% !important;
}

/* 隐藏原标题 emoji 与文字，替换为英文 */
.chat-thought-title {
  font-size: 0;
}
.chat-thought-title::before {
  content: "Inner Monologue";
  font-size: 14px;
  font-weight: 500;
  color: #4B5563;
}

/* 正文风格 */
.chat-thought-body {
  color: #4B5563;
  font-size: 14px;
  line-height: 1.6;
}

/* 签名样式 */
.chat-thought-sig {
  text-align: right;
  color: #9CA3AF;
  font-size: 12px;
  margin-top: 10px;
  font-style: italic;
}

/* ── 引用条（普通气泡内） ── */
.chat-bubble-role-assistant:not(.chat-bubble-media):not(.voice-msg-bubble),
.chat-bubble-role-user:not(.chat-bubble-media):not(.voice-msg-bubble) {
  position: relative;
  overflow: visible !important;
}

.chat-quote-preview {
  position: absolute;
  top: 1px;                                       /* 👈与气泡距离 */
  max-width: 120px;                               /* 👈最大宽度 */
  margin: 0 !important;
  background: none !important;
  border: none !important;
  padding: 0;
  display: block;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  font-size: 12px;
  line-height: 1.3;
  color: var(--c-text, #797E85);
  z-index: 2;
}
.chat-quote-preview::before {
  content: "回覆";                                /* 👈此处可修改文字 */
  display: block;
  font-size: 10px;
  font-weight: 700;
  color: var(--c-icon, #A0A3A8);
  letter-spacing: 0.5px;
  line-height: 1;
  margin-bottom: 2px;
}

/* AI 引用条位置 */
.chat-bubble-role-assistant .chat-quote-preview {
  left: calc(100% + 8px);
  color: var(--c-text, #797E85);
}
/* 用户引用条位置 */
.chat-bubble-role-user .chat-quote-preview {
  right: calc(100% + 8px);
  color: #6B7280;                                 /* 👈预览文字颜色 */
}
.chat-bubble-role-user .chat-quote-preview::before {
  color: #9CA3AF;                                 /* 👈标签文字颜色 */
}

/* ── 去除系统消息背景 ── */
.chat-sys-msg {
  background: none !important;
  border: none !important;
  box-shadow: none !important;
}

/* ── 时间戳 ── */
span.chat-sys-msg {
  position: relative;
  display: inline-flex;
  align-items: center;
  overflow: visible;
}

/* ── 语音转文字气泡 ── */
.chat-msg-wrapper[data-role="user"] .voice-msg-text-bubble,
.chat-msg-wrapper[data-role="assistant"] .voice-msg-text-bubble {
  margin-top: 2px !important;
  margin-bottom: 0 !important;

  /* 超透磨砂玻璃背景 */
  background: rgba(255, 255, 255, 0.03) !important;
  backdrop-filter: blur(1px) !important;
  -webkit-backdrop-filter: blur(1px) !important;
  border-radius: 10px;
  padding: 6px 18px !important;
  position: relative;
  border: none !important;
}

/* 左引号 */
.chat-msg-wrapper[data-role="user"] .voice-msg-text-bubble::before,
.chat-msg-wrapper[data-role="assistant"] .voice-msg-text-bubble::before {
  content: "❝";
  position: absolute;
  left: 4px;
  top: 6px;
  font-size: 16px;
  color: rgba(0,0,0,0.3);
  line-height: 1;
  pointer-events: none;
}

/* 右引号 */
.chat-msg-wrapper[data-role="user"] .voice-msg-text-bubble::after,
.chat-msg-wrapper[data-role="assistant"] .voice-msg-text-bubble::after {
  content: "❞";
  position: absolute;
  right: 4px;
  bottom: 2px;
  font-size: 16px;
  color: rgba(0,0,0,0.3);
  line-height: 1;
  pointer-events: none;
}

/* 独立 msg-wrapper 间距锁定 */
.chat-msg-wrapper:has(.voice-msg-text-bubble) {
  margin-top: -2px !important;
  margin-bottom: 0 !important;
}

/* ========== 底部栏毛玻璃底色：偏白半透明 ========== */
.chat-input-bar {
  background-color: rgba(255, 255, 255, 0.85) !important;  /* 偏白，带一点通透感 */
  backdrop-filter: blur(12px) !important;
  -webkit-backdrop-filter: blur(12px) !important;
}

/* ========== 输入框：透明背景、无边框、方形尖角 ========== */
.chat-input-bar .chat-input-textarea {
  border: none !important;
  border-radius: 0 !important;
  background: transparent !important;
  outline: none;
  padding: 10px 12px;
  font-size: 16px;               /* 输入字体大小：从 14px 调大到 16px */
  color: #252525;
  width: 100%;
  box-sizing: border-box;
  resize: none;
  margin-top: -2px;
  line-height: 1.4;              /* 适当增加行高，保证多行输入时舒适 */
}

.chat-input-bar .chat-input-textarea::placeholder {
  color: var(--c-text, #797E85);
  opacity: 0.55;
}

/* ========== 按钮区：按钮整体靠右，背景透明，顶部留线空间 ========== */
.chat-input-bar .chat-input-actions {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10px;
  padding: 18px 10px 6px;
  background: transparent;
  --line-left: 162px;            /* 下框线起点 */
}

/* 左侧字母：与线同一水平线，左边缘与输入框文字对齐，加粗 */
.chat-input-bar .chat-input-actions::before {
  content: "F L O A T P H O N E";
  position: absolute;
  top: 0;
  left: 12px;
  transform: translateY(-50%);
  font-size: 10px;
  letter-spacing: 0.28em;
  color: var(--c-text, #797E85);
  line-height: 1;
  white-space: nowrap;
  z-index: 1;
  font-weight: bold;
}

/* 下框线：从字母右侧开始，颜色与字母一致 */
.chat-input-bar .chat-input-actions::after {
  content: "";
  position: absolute;
  top: 0;
  left: var(--line-left);
  right: 0;
  height: 1px;
  background: var(--c-text, #797E85);
  z-index: 0;
}

/* ========== 普通按钮：自然宽度，无背景，悬浮/点击颜色 #252525 ========== */
.chat-input-bar .chat-input-actions .ui-bare-btn {
  flex: 0 0 auto;
  min-width: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 4px 2px;
  margin: 0;
  border: none;
  background: transparent;
  color: var(--c-text, #797E85);
  cursor: pointer;
  transition: color 0.2s ease, transform 0.1s ease;
}

.chat-input-bar .chat-input-actions .ui-bare-btn:hover {
  color: #252525;
}

.chat-input-bar .chat-input-actions .ui-bare-btn:active {
  color: #252525;
  transform: scale(0.92);
}

.chat-input-bar .chat-input-actions .ui-bare-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  pointer-events: none;
}

/* ========== 精准选中“触发 AI 回复”按钮，替换为圆形图片图标 ========== */
.chat-input-bar .chat-input-actions .ui-bare-btn:has(svg path[d^="M9.937 15.5"]) {
  width: 28px;
  height: 28px;
  padding: 0;
  background: transparent url("https://i.ibb.co/hJ6gkpMD/IMG-20260408-220056.jpg") no-repeat center center / cover;
  border-radius: 50%;
  border: none;
  box-sizing: border-box;
  flex-shrink: 0;
}

/* 隐藏该按钮原来的 SVG 图标 */
.chat-input-bar .chat-input-actions .ui-bare-btn:has(svg path[d^="M9.937 15.5"]) svg {
  display: none;
}

.chat-input-bar .chat-input-actions .ui-bare-btn:has(svg path[d^="M9.937 15.5"]):hover {
  opacity: 0.8;
}

.chat-input-bar .chat-input-actions .ui-bare-btn:has(svg path[d^="M9.937 15.5"]):active {
  opacity: 0.6;
  transform: scale(0.92);
}

/* ========== 隐藏引用条的下框线 ========== */
.chat-quote-bar {
  border-bottom: none !important;
  box-shadow: none !important;
}

/* ========== 线下模式按钮 ========== */
.chat-input-bar .chat-input-actions .chat-offline-toggle {
  margin-right: auto;            /* 推到最左侧，其他按钮保持靠右 */
  position: relative;
  padding: 2px;
  display: flex;
  align-items: center;
  justify-content: center;
  transform: translateY(7px);    /* 整体下移量，控制上方文字位置 */
  --icon-drop: 4px;              /* 图标和“浮岛”额外下移量 */
}

/* 调小线下模式按钮的图标，并额外下移 */
.chat-input-bar .chat-input-actions .chat-offline-toggle svg {
  width: 12px;
  height: 12px;
  transform: translateY(var(--icon-drop));
}

/* 图标上方小字 */
.chat-input-bar .chat-input-actions .chat-offline-toggle::before {
  content: "编辑于 今天13:14";
  position: absolute;
  bottom: 100%;
  left: 0;
  font-size: 12px;
  color: var(--c-text, #797E85);
  white-space: nowrap;
  line-height: 1;
  pointer-events: none;
}

/* 图标右侧小字 */
.chat-input-bar .chat-input-actions .chat-offline-toggle::after {
  content: "浮岛";
  position: absolute;
  left: calc(100% + 4px);
  top: calc(50% + var(--icon-drop));
  transform: translateY(-50%);
  font-size: 12px;
  color: var(--c-text, #797E85);
  white-space: nowrap;
  line-height: 1;
  pointer-events: none;
}
